# Copie para .env e ajuste. .env não é versionado.

DATABASE_URL=postgres://draconya:draconya@localhost:5432/draconya
REDIS_URL=redis://localhost:6379

# Processo api
API_PORT=3000
API_ORIGIN=http://localhost:5173

# Processo game
GAME_PORT=7171
GAME_PUBLIC_URL=ws://localhost:7171

# Autenticação (WorkOS). Deixe vazio para usar o modo de desenvolvimento,
# que aceita qualquer e-mail sem verificar — nunca ligue isso em produção.
WORKOS_API_KEY=
WORKOS_CLIENT_ID=
AUTH_DEV_MODE=true

# Pacote de assets do cliente Tibia. Não versionado (things/ está no .gitignore).
THINGS_VERSION=1332
THINGS_DIR=./things

LOG_LEVEL=debug
